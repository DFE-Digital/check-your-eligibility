{
    "configurations": [
        {
            "type": "coreclr",
            "request": "launch",
            "name": "Launch CheckYourEligibility.API",
            "program": "${workspaceFolder}/CheckYourEligibility.API/bin/Debug/net8.0/CheckYourEligibility.API.dll",
            "args": [],
            "cwd": "${workspaceFolder}/CheckYourEligibility.API",
            "stopAtEntry": false,
            "preLaunchTask": "build"
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "programPath",
            "description": "Path to your program"
        }
    ]
}