trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: Bash@3
  displayName: 'Bash Install'
  inputs:
   targetType: inline
   script: |
     ls
     cd tests
     ls
     npm install

- task: Bash@3
  displayName: 'run cypress tests'
  inputs:
   targetType: inline
   script: |
    cd tests
    npm e2e:chrome